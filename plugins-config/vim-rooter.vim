" To specify the root has a certain directory or file (which may be a glob), just give the name:
let g:rooter_patterns = ['Gemfile', 'deploy.bat', 'notes.md', '.git', 'Makefile', '*.sln', 'build/env.sh']
